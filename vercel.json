{
    "version": 2,
    "builds": [
        {
            "src": "backend/src/server.js",
            "use": "@now/node"
        },
        {
            "src": "frontend/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        { "src": "/backend/(.*)", "dest": "src/server.js" }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}